#server configs
server.port=8082

# create and drop tables and sequences, loads import.sql
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# H2 settings
#spring.datasource.url=jdbc:postgresql://${DATABASE_HOST}/${DATABASE_NAME}
#spring.datasource.url=jdbc:postgresql://localhost/pgdb_profiling
#spring.datasource.username=${DATABASE_USERNAME:postgres}
#spring.datasource.password=${DATABASE_PASSWORD:Kazakhstan1991}

spring.datasource.url=jdbc:postgresql://localhost/pgdb_unlinkedin
spring.datasource.username=postgres
spring.datasource.password=Kazakhstan1991